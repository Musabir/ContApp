<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:myapp="http://schemas.android.com/apk/res-auto"

    >
    <item
        android:id="@+id/action_searcha"
        myapp:showAsAction="always"
        myapp:actionViewClass="android.widget.SearchView"
        android:title="Search" />

    <item android:id="@+id/filter"
        android:icon="@drawable/ic_filter"
        android:title="@string/filter"
        android:numericShortcut="10"
        myapp:showAsAction="always">


        <menu>
            <item
                android:id="@+id/spinner"
                android:title="Region"
                myapp:showAsAction="always"
                >
                <menu>
                </menu>
            </item>
            <item
                android:id="@+id/only_tollfree"
                android:title="Only Tollfree"
                myapp:showAsAction="always"
                android:numericShortcut="12"
                android:checkable="true"/>


        </menu>
    </item>

   </menu>